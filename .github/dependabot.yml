version: 2
updates:
  # Actions are pinned to commit SHAs for supply-chain safety, which means they never
  # update on their own. This is what keeps them from rotting.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: ci
    open-pull-requests-limit: 3
