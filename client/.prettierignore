node_modules/
dist/
.expo/
nativewind-env.d.ts
score-web/
src/score-web/html.ts

# Generated by release-please on every release PR. Its markdown (`*` bullets, double
# blank lines) does not match Prettier's, and it rewrites the file each run — policing
# it here would fail CI on every release PR forever.
CHANGELOG.md
